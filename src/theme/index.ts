import {createTheme} from "@mui/material";

export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: ""
    },
    primary: {
      main: "#f143e2",
      dark: "#40187f"
    },
    info: {
      main: "#31C26A"
    }
  },

})
